[build]
  command   = "npm run build"
  publish   = "dist"

# Client-side routing: any path the build did not emit is an app route.
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
